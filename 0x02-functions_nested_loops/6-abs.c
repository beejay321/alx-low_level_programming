#include "main.h"
#include <stdio.h>
/**
 * _abs - function that computes the absolute value of an integer
 * @r: a number
 * Description: 0
 * Return: 0
 *
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (-r);
}
